# Config Templates

This folder contains safe example files only.

Real config files may contain passwords, hashes, secrets, proxy credentials, Wi-Fi names, or device metadata. Keep real files on the Raspberry Pi and do not commit them to a public repository.

