# Copy this file to proxy.env on the Raspberry Pi if proxy mode is used.
# Do not commit real proxy credentials.

PROXY_TYPE=socks5
PROXY_HOST=example.proxy.local
PROXY_PORT=1080
PROXY_USERNAME=
PROXY_PASSWORD=

