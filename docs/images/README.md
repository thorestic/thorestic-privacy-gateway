# Images

Included images:

- `dashboard-preview.png`: screenshot from the real dashboard UI running locally.
- `network-page.png`: screenshot from the real Network page running locally.

Before publishing, you can add real screenshots:

- `hardware-setup.jpg`: a photo of the Raspberry Pi/router setup.

Do not show passwords, real proxy settings, private SSIDs, private client names, MAC addresses, logs, or public IP addresses in screenshots.
